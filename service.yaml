apiVersion: v1
kind: Service
metadata:
  name: app:AppCachingReddit-cd
  labels:
    app: app:AppCachingReddit-cd
spec:
  selector:
    app: app:AppCachingReddit-cd

  ports:
    - port: 3000
      targetPort: 3000

  type: LoadBalancer
